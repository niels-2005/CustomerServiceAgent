"""Top-level package entrypoint helpers for the customer bot project."""

from __future__ import annotations


def main() -> None:
    """Print the available CLI entrypoints for local usage."""
    print("Use `customer-bot-api` to run the API or `customer-bot-ingest` to ingest FAQs.")
